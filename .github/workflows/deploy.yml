name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install rsync (just in case)
        run: sudo apt-get update && sudo apt-get install -y rsync

      - name: Copy files to VPS (exclude .env & node_modules)
        env:
          SSH_PRIVATE_KEY: ${{ secrets.VPS_SSH_DEPLOY_KEY }}
          VPS_IP: ${{ secrets.VPS_IPV4 }}
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H $VPS_IP >> ~/.ssh/known_hosts

          # Synchronisation sans écraser .env et node_modules
          rsync -avz --delete \
            --exclude='.env' \
            --exclude='node_modules' \
            ./ ubuntu@$VPS_IP:/var/www/discorev-back/

      - name: Install dependencies, migrate DB and restart server
        env:
          SSH_PRIVATE_KEY: ${{ secrets.VPS_SSH_DEPLOY_KEY }}
          VPS_IP: ${{ secrets.VPS_IPV4 }}
        run: |
          ssh -i ~/.ssh/id_rsa ubuntu@$VPS_IP << 'EOF'
            cd /var/www/discorev-back

            echo "📦 Installation des dépendances..."
            npm install --production
            
            echo "🔄 Lancement des migrations..."
            npm run migrate

            echo "🚀 Redémarrage du serveur..."
            npx pm2 restart discorev || npx pm2 start server.js --name discorev
          EOF
